Для запуска приложения необходимо выполнить команду `uvicorn backend.main:app --reload`

Для запуска тестов необходимо выполнить команду `pytest` .Обязательно надо выполнить команду из директории backend